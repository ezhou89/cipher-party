# Cipher Party

Cipher Party is a private, account-free multiplayer association game.

## Development

Use Node.js 22 or newer, then install dependencies and start the local web and Worker processes:

```bash
npm install
npm run dev
```

Run the repository quality gate with `npm run check` and build all workspaces with `npm run build`.
