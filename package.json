{
  "name": "cipher-party",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter @cipher-party/game-core run build && pnpm --filter @cipher-party/protocol run build && pnpm --filter @cipher-party/web run build && pnpm --filter @cipher-party/worker run build",
    "check": "pnpm run check:docs && pnpm run format:check && pnpm run lint && pnpm run typecheck && pnpm run test",
    "check:release": "pnpm run check && pnpm run check:complexity && pnpm run test:coverage && pnpm run build && pnpm run preflight && pnpm run test:e2e",
    "check:complexity": "node scripts/check-complexity.mjs",
    "check:release:clean": "node scripts/check-clean-release.mjs",
    "check:docs": "node scripts/check-project-docs.mjs",
    "check:staging": "node scripts/check-staging.mjs",
    "deploy:staging": "node scripts/release-staging.mjs",
    "dev": "concurrently -k -n worker,web \"pnpm --filter @cipher-party/worker run dev\" \"pnpm --filter @cipher-party/web run dev\"",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "preflight": "node scripts/preflight.mjs",
    "smoke:staging:public": "playwright test --config playwright.staging.config.ts",
    "test": "pnpm -r --if-present run test && vitest run --config vitest.config.ts",
    "test:coverage": "pnpm --filter @cipher-party/game-core run test:coverage && pnpm --filter @cipher-party/protocol run test:coverage && pnpm --filter @cipher-party/web run test:coverage && pnpm --filter @cipher-party/worker run test:coverage && vitest run --config vitest.config.ts --coverage",
    "test:e2e": "playwright test",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "@cipher-party/protocol": "workspace:*",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.0",
    "@vitest/coverage-istanbul": "4.1.11",
    "concurrently": "^10.0.5",
    "eslint": "^10.9.1",
    "fast-check": "^4.9.0",
    "jsonc-parser": "^3.3.1",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.68.0",
    "vitest": "^4.1.11"
  }
}
